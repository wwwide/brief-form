import { ReactElement } from 'react';
import { FormFieldProps } from '../types';
export declare const FieldRenderer: <ValueType, InputProps>(props: FormFieldProps<ValueType, InputProps>) => ReactElement;
